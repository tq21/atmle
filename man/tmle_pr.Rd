% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmle_pr.R
\name{tmle_pr}
\alias{tmle_pr}
\title{TMLE Pooled Regression for RCT + RWD Data Fusion}
\format{
An object of class \code{R6ClassGenerator}.
}
\usage{
tmle_pr
}
\description{
R6 class implementing a pooled-regression TMLE for average treatment effects
using randomized trial and real-world data.
}
\details{
The estimator fits a pooled outcome regression \eqn{E(Y \mid S, W, A)}, a
trial treatment mechanism \eqn{P(A = 1 \mid S = 1, W)}, and a trial
participation mechanism \eqn{P(S = 1 \mid W)}. It then targets the outcome
regression for two ATE parameters:

\itemize{
  \item Average over the pooled covariate distribution.
  \item Average over the trial-only covariate distribution (\eqn{S=1}).
}
}
\section{Initialization}{

Create the object with:

\code{tmle_pr$new(data, S_node, W_nodes, A_node, Y_node, family, n_folds, seed)}
}

\section{Main Method (\code{$run()})}{

The primary workflow is \code{fit$run(Q_method, g_method, Pi_bar_method, ...)},
where each method is an \code{sl3} learner specification accepted by
\code{fit_regression()}.
}

\value{
An R6 class generator object. Use \code{$new(...)} to instantiate.
}
